services:
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: carhero_frontend
    ports:
      - "5173:5173"
    volumes:
      - ./frontend:/app:cached
    depends_on:
      - backend
    environment:
      VITE_API_URL: http://localhost:8000
      VITE_APP_NAME: TFG-CAR-HERO
      VITE_ENV: development
      VITE_PAGINATION_LIMIT: 10
    command: ["sh", "-c", "npm install && npm run dev -- --host"]

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: carhero_backend
    ports:
      - "8000:8000"
    volumes:
      - ./backend:/var/www/html
    depends_on:
      - db
    environment:
      DB_CONNECTION: mysql
      DB_HOST: db
      DB_PORT: 3306
      DB_DATABASE: db
      DB_USERNAME: user
      DB_PASSWORD: 12345?
      FRONTEND_URL: http://localhost:5173
    command:
      sh -c "until nc -z db 3306; do echo 'Esperando a que MySQL esté listo...'; sleep 2; done; php artisan optimize:clear;
      exec php -S 0.0.0.0:8000 -t public"

  db:
    image: mysql:8.0
    container_name: carhero_mysql
    restart: always
    environment:
      MYSQL_DATABASE: db
      MYSQL_USER: user
      MYSQL_PASSWORD: 12345?
      MYSQL_ROOT_PASSWORD: root
    ports:
      - "3306:3306"
    volumes:
      - db_data:/var/lib/mysql

  phpmyadmin:
    image: phpmyadmin/phpmyadmin
    container_name: carhero_phpmyadmin
    restart: always
    ports:
      - "8081:80"
    environment:
      PMA_HOST: db
      PMA_PORT: 3306
    depends_on:
      - db

volumes:
  db_data:
